import { ShieldCheck } from "lucide-react";
import { requireActiveViewer } from "@/lib/auth/session";
import { createClient } from "@/lib/supabase/server";
import { createPrivacyRequestAction } from "./actions";
import styles from "../../operations.module.css";

export default async function PrivacyPage({searchParams}:{searchParams:Promise<{erro?:string;sucesso?:string}>}){const viewer=await requireActiveViewer();const feedback=await searchParams;const supabase=await createClient();const[{data:requests},{data:policies},{data:contacts}]=await Promise.all([supabase.from("privacy_requests").select("*,contacts(name)").eq("org_id",viewer.organization!.id).order("created_at",{ascending:false}),supabase.from("retention_policies").select("*").eq("org_id",viewer.organization!.id),supabase.from("contacts").select("id,name").eq("org_id",viewer.organization!.id).eq("status","active").order("name").limit(500)]);return <div className={styles.page}><header className={styles.header}><div><p className={styles.eyebrow}>Privacidade e retenção</p><h1>Solicitações de titular</h1><p>Exclusão pode ser suspensa por obrigação legal; conclusão exige prova do worker de purge.</p></div><ShieldCheck/></header>{feedback.erro?<p className={styles.error}>Não foi possível abrir a solicitação.</p>:null}{feedback.sucesso?<p className={styles.success}>Solicitação aberta para revisão.</p>:null}<section className={styles.metrics}>{policies?.map((policy)=><div className={styles.metric} key={policy.id}><small>{policy.data_class}</small><strong>{policy.retention_days} dias</strong><span>{policy.action}</span></div>)}</section><div className={styles.layout}><main><section className={styles.panel}><div className={styles.panelHeader}><h2>Fila</h2><span>{requests?.length??0}</span></div><div className={styles.list}>{requests?.map((request)=>{const contact=request.contacts as {name:string}|null;return <article className={styles.item} key={request.id}><span><strong>{contact?.name??"Contato"}</strong><small>{request.request_type} · vence {new Date(request.due_at).toLocaleDateString("pt-BR")}</small></span><b>{request.status}</b></article>})}{!requests?.length?<p className={styles.empty}>Nenhuma solicitação.</p>:null}</div></section></main><aside><form action={createPrivacyRequestAction} className={styles.formCard}><h2>Nova solicitação</h2><label><span>Contato</span><select name="contactId"><option value="">Selecione</option>{contacts?.map((contact)=><option key={contact.id} value={contact.id}>{contact.name}</option>)}</select></label><label><span>Tipo</span><select name="requestType"><option value="access">Acesso</option><option value="export">Exportação</option><option value="correction">Correção</option><option value="restriction">Restrição</option><option value="deletion">Exclusão</option><option value="anonymization">Anonimização</option></select></label><button>Abrir solicitação</button></form></aside></div></div>}
